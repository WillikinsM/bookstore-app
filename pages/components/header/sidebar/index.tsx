/* import { Stack, Text } from "@chakra-ui/react";

const Sidebar = (props: boolean) => {
  return (
    <Stack
      direction={{ base: "column", md: "row" }}
      display={{ base: props ? "block" : "none", md: "flex" }}
      width={{ base: "full", md: "auto" }}
      alignItems="center"
      flexGrow={1}
      mt={{ base: 4, md: 0 }}
    >
      <Text>Docs</Text>
      <Text>Examples</Text>
      <Text>Blog</Text>
    </Stack>
  );
};

export default Sidebar;
 */
export {}